import 'package:flutter/material.dart';

class AllgemeinesPage extends StatelessWidget {
  const AllgemeinesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('Allgemeines'),
      ),
    );
  }
}